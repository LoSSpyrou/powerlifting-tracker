/// Window widths at or above this switch from a bottom [NavigationBar] to a
/// side [NavigationRail] -- wide enough to spare a permanent side column
/// without crowding the content, matching Material's medium-window
/// breakpoint.
const double kLargeScreenMinWidth = 600.0;

/// Caps the width of forms, lists, and charts so they read as a fixed
/// station on a wide competition-platform display rather than stretching
/// edge-to-edge on a desktop-sized window.
const double kContentMaxWidth = 640.0;
